# Инструкция по загрузке проекта на GitHub

## Шаг 1: Создание репозитория на GitHub

1. Перейдите на https://github.com/
2. Нажмите кнопку "New" (зеленая кнопка справа вверху)
3. Заполните форму:
   - **Repository name**: `masters-platform`
   - **Description**: `Веб-платформа для поиска мастеров на Django (Курсовая работа)`
   - **Public** или **Private** (на ваш выбор)
   - ❌ НЕ добавляйте README, .gitignore или LICENSE (они уже есть в проекте)
4. Нажмите "Create repository"

## Шаг 2: Инициализация Git в проекте

Откройте терминал в папке проекта и выполните:

```bash
# Инициализация Git
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Django platform for finding masters"

# Переименование ветки в main (если нужно)
git branch -M main
```

## Шаг 3: Подключение к GitHub

Замените `yourusername` на ваше имя пользователя GitHub:

```bash
git remote add origin https://github.com/yourusername/masters-platform.git
```

## Шаг 4: Загрузка на GitHub

```bash
git push -u origin main
```

Если потребуется аутентификация:
- Введите ваш GitHub username
- Вместо пароля используйте Personal Access Token (см. ниже)

## Создание Personal Access Token (если нужно)

1. Перейдите на https://github.com/settings/tokens
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Дайте имя токену: `masters-platform-token`
4. Выберите срок действия: 90 days
5. Отметьте галочки:
   - ✅ `repo` (все подпункты)
6. Нажмите "Generate token"
7. **ВАЖНО**: Скопируйте токен и сохраните его (он больше не будет показан)
8. Используйте этот токен вместо пароля при push

## Шаг 5: Проверка

Откройте ваш репозиторий на GitHub:
```
https://github.com/yourusername/masters-platform
```

Вы должны увидеть все файлы проекта!

## Шаг 6: Настройка README на GitHub

GitHub автоматически отобразит `README.md` на главной странице репозитория.

## Шаг 7: Добавление тем (Topics)

На странице репозитория:
1. Нажмите на шестеренку рядом с "About"
2. Добавьте темы (topics):
   - `django`
   - `python`
   - `web-platform`
   - `property-based-testing`
   - `spec-driven-development`
   - `coursework`
3. Сохраните

## Структура репозитория на GitHub

После загрузки ваш репозиторий будет выглядеть так:

```
masters-platform/
├── 📄 README.md                    # Главная страница
├── 📄 LICENSE                      # Лицензия MIT
├── 📄 requirements.txt             # Зависимости
├── 📄 .gitignore                   # Исключения Git
├── 📁 .kiro/specs/                 # Спецификации
├── 📁 accounts/                    # Приложение пользователей
├── 📁 orders/                      # Приложение заказов
├── 📁 portfolio/                   # Приложение портфолио
├── 📁 reviews/                     # Приложение отзывов
├── 📁 notifications/               # Приложение уведомлений
├── 📁 core/                        # Общие утилиты
├── 📁 templates/                   # HTML шаблоны
├── 📁 fixtures/                    # Начальные данные
└── 📁 masters_platform/            # Настройки Django
```

## Дополнительные команды Git

### Добавление изменений:
```bash
git add .
git commit -m "Описание изменений"
git push
```

### Просмотр статуса:
```bash
git status
```

### Просмотр истории:
```bash
git log --oneline
```

### Создание новой ветки:
```bash
git checkout -b feature/new-feature
```

### Переключение между ветками:
```bash
git checkout main
```

## Настройка GitHub Pages (опционально)

Если хотите разместить документацию:

1. Перейдите в Settings → Pages
2. Source: Deploy from a branch
3. Branch: main, folder: /docs (если создадите папку docs)
4. Сохраните

## Добавление соавторов (если работаете в команде)

1. Settings → Collaborators
2. Add people
3. Введите username или email

## Защита ветки main (рекомендуется)

1. Settings → Branches
2. Add rule
3. Branch name pattern: `main`
4. Отметьте:
   - ✅ Require pull request reviews before merging
   - ✅ Require status checks to pass before merging
5. Сохраните

## Создание Release (когда проект готов)

1. Перейдите на вкладку "Releases"
2. Нажмите "Create a new release"
3. Tag version: `v1.0.0`
4. Release title: `Версия 1.0 - Первый релиз`
5. Описание: краткое описание функциональности
6. Нажмите "Publish release"

## Полезные ссылки

- **GitHub Docs**: https://docs.github.com/
- **Git Cheat Sheet**: https://education.github.com/git-cheat-sheet-education.pdf
- **Markdown Guide**: https://www.markdownguide.org/

## Проблемы и решения

### Ошибка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/yourusername/masters-platform.git
```

### Ошибка: "failed to push some refs"
```bash
git pull origin main --rebase
git push origin main
```

### Забыли добавить .gitignore
```bash
git rm -r --cached .
git add .
git commit -m "Update .gitignore"
git push
```

## Готово! 🎉

Ваш проект теперь на GitHub и доступен по адресу:
```
https://github.com/yourusername/masters-platform
```

Поделитесь ссылкой с преподавателем или коллегами!
