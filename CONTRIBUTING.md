# Руководство по внесению вклада

Спасибо за интерес к проекту! Этот документ описывает процесс внесения изменений.

## Процесс разработки

Проект следует методологии Spec-Driven Development:

1. **Требования** → 2. **Проектирование** → 3. **Реализация** → 4. **Тестирование**

## Перед началом работы

1. Изучите спецификацию в `.kiro/specs/masters-platform/`
2. Прочитайте `SPEC_GUIDE.md` для понимания структуры
3. Убедитесь, что у вас установлены все зависимости

## Стандарты кодирования

### Python/Django
- Следуйте PEP 8
- Используйте type hints где возможно
- Документируйте функции и классы
- Максимальная длина строки: 100 символов

### Именование
- Модели: `CamelCase` (например, `CustomUser`)
- Функции/методы: `snake_case` (например, `create_notification`)
- Константы: `UPPER_CASE` (например, `MAX_FILE_SIZE`)

### Комментарии
- Пишите docstrings для всех публичных функций
- Используйте комментарии для сложной логики
- Комментарии на русском языке

## Процесс внесения изменений

### 1. Создание ветки

```bash
git checkout -b feature/your-feature-name
```

Типы веток:
- `feature/` - новая функциональность
- `bugfix/` - исправление ошибок
- `hotfix/` - срочные исправления
- `test/` - добавление тестов

### 2. Разработка

1. Найдите соответствующую задачу в `tasks.md`
2. Реализуйте функциональность
3. Напишите тесты (unit и property-based)
4. Убедитесь, что все тесты проходят

### 3. Тестирование

```bash
# Запуск всех тестов
pytest

# Проверка покрытия
pytest --cov=. --cov-report=html

# Запуск property-based тестов
pytest -m property
```

Минимальное покрытие: 80%

### 4. Коммит

Формат сообщения коммита:

```
<type>: <subject>

<body>

Refs: #<issue-number>
```

Типы:
- `feat`: новая функциональность
- `fix`: исправление ошибки
- `test`: добавление тестов
- `docs`: документация
- `refactor`: рефакторинг
- `style`: форматирование

Пример:
```
feat: добавлена система уведомлений

Реализована модель Notification и представления для
отображения и управления уведомлениями.

Refs: #15
```

### 5. Push и Pull Request

```bash
git push origin feature/your-feature-name
```

Создайте Pull Request с описанием:
- Что было сделано
- Какие требования реализованы
- Какие тесты добавлены
- Скриншоты (если применимо)

## Требования к Pull Request

### Обязательно:
- [ ] Все тесты проходят
- [ ] Покрытие кода не снизилось
- [ ] Код соответствует стандартам
- [ ] Добавлены docstrings
- [ ] Обновлена документация (если нужно)

### Для новой функциональности:
- [ ] Добавлены unit тесты
- [ ] Добавлены property-based тесты (если применимо)
- [ ] Обновлен `tasks.md` (отмечена выполненная задача)
- [ ] Добавлены миграции (если изменялись модели)

### Для исправления ошибок:
- [ ] Добавлен тест, воспроизводящий ошибку
- [ ] Тест проходит после исправления
- [ ] Описана причина ошибки

## Написание тестов

### Unit тесты

```python
import pytest
from django.test import TestCase

class OrderModelTest(TestCase):
    def test_order_creation(self):
        """Тест создания заказа"""
        order = Order.objects.create(
            customer=self.user,
            title="Test Order",
            # ...
        )
        assert order.status == 'active'
```

### Property-based тесты

```python
from hypothesis import given, strategies as st

@given(st.text(min_size=1, max_size=200))
@pytest.mark.property
def test_order_title_property(title):
    """
    Feature: masters-platform, Property 7: Order creation
    Validates: Requirements 3.1
    """
    order = create_order(title=title)
    assert order.title == title
```

## Структура тестов

```
app_name/
├── tests/
│   ├── __init__.py
│   ├── test_models.py      # Unit тесты моделей
│   ├── test_views.py       # Unit тесты представлений
│   ├── test_forms.py       # Unit тесты форм
│   └── test_properties.py  # Property-based тесты
```

## Миграции

### Создание миграций:
```bash
python manage.py makemigrations
```

### Проверка миграций:
```bash
python manage.py migrate --plan
```

### Именование миграций:
Django автоматически генерирует имена, но вы можете указать свое:
```bash
python manage.py makemigrations --name add_rating_field
```

## Документация

### Обновление README.md
- Добавьте новые функции в раздел "Основные функции"
- Обновите инструкции по установке (если нужно)

### Обновление SPEC_GUIDE.md
- Добавьте новые свойства (если добавлены)
- Обновите примеры (если нужно)

## Проверка перед отправкой

```bash
# Форматирование кода
black .

# Проверка стиля
flake8 .

# Запуск тестов
pytest

# Проверка миграций
python manage.py makemigrations --check --dry-run
```

## Вопросы и помощь

- Создайте issue для обсуждения больших изменений
- Используйте Discussions для вопросов
- Проверьте существующие issues перед созданием нового

## Лицензия

Внося вклад в проект, вы соглашаетесь с условиями лицензии проекта.

Спасибо за ваш вклад! 🎉
